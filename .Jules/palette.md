## 2024-05-22 - [HTML Structure Fundamentals]
**Learning:** Even well-structured frameworks can have missing basic HTML tags (like `<body>`), which breaks accessibility tools and basic semantic parsing. Always verify the root template structure before assuming high-level components will work correctly.
**Action:** Always check layout files for `<html>`, `<body>`, and `main` tags before implementing deeper a11y features.
