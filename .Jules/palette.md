## 2026-01-17 - Login Form Accessibility
**Learning:** Adding standard HTML5 validation attributes (`required`, `autocomplete`) and proper ARIA error associations (`aria-describedby`) significantly improves the accessibility of authentication forms without requiring complex JavaScript or design changes.
**Action:** Always verify `autocomplete` attributes on email and password fields to support password managers and screen readers.
